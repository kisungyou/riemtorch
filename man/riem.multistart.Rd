% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver-runtime.R
\name{riem.multistart}
\alias{riem.multistart}
\title{Run Reproducible Multiple Starts on One Device}
\usage{
riem.multistart(
  problem,
  initials = NULL,
  starts = 5L,
  seed = 1L,
  method = "steepest_descent",
  control = list(),
  device = NULL,
  dtype = NULL
)
}
\arguments{
\item{problem}{An optimization problem.}

\item{initials}{Optional list of feasible initial points.}

\item{starts}{Number of random starts when initials is NULL.}

\item{seed}{Integer initialization seed. NULL uses the current RNG stream.}

\item{method, control}{Solver and controls passed to [riem.optimize()].}

\item{device, dtype}{Execution requests resolved once for all starts.}
}
\value{
A list of fits, the index of the best finite feasible fit, its fit,
  and execution metadata. Failed fits remain in the result.
}
\description{
Run Reproducible Multiple Starts on One Device
}
\examples{
if (torch::torch_is_installed()) {
  P <- riem.problem(manifold.sphere(3),function(x) -x[1])
  riem.multistart(P,starts=2,device="cpu")$best$objective
}
}
