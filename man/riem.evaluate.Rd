% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem-core.R
\name{riem.evaluate}
\alias{riem.evaluate}
\alias{riem.hessian}
\title{Evaluate Values and Metric Derivatives}
\usage{
riem.evaluate(problem, x, gradient = TRUE)

riem.hessian(problem, x, u)
}
\arguments{
\item{problem}{A scalar, finite-sum or least-squares problem.}

\item{x}{A feasible point without batch axes.}

\item{gradient}{Whether to compute the gradient.}

\item{u}{Tangent direction for a Hessian-vector product.}
}
\value{
`riem.evaluate()` returns value, optional metric gradient and counts.
  `riem.hessian()` returns an exact Riemannian Hessian-vector product.
}
\description{
Evaluate Values and Metric Derivatives
}
\details{
Automatic Hessian conversion is available for Euclidean, sphere,
torus, oblique, Euclidean-metric Stiefel, both Grassmann representations,
generalized Stiefel/Grassmann, rotations, AIRM SPD, and weighted products of
supported factors, when the objective supports double backward. Objectives
using custom first-order matrix logarithm/root kernels require an analytic
`rhess` for exact second-order methods.
}
\examples{
if (torch::torch_is_installed()) {
  P <- riem.problem(manifold.euclidean(2), function(x) torch::torch_sum(x^2)/2)
  x <- torch::torch_tensor(c(1, 2), dtype = torch::torch_float64())
  riem.evaluate(P, x)
  riem.hessian(P, x, x)
}
}
