% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{riem.check.manifold}
\alias{riem.check.manifold}
\alias{riem.check.gradient}
\alias{riem.check.hessian}
\alias{riem.check.adjoint}
\title{Check a Manifold or User-Supplied Derivatives}
\usage{
riem.check.manifold(manifold, x = NULL, u = NULL, tolerance = NULL)

riem.check.gradient(problem, x, u = NULL, steps = NULL, tolerance = NULL)

riem.check.hessian(problem, x, u = NULL, steps = NULL, tolerance = NULL)

riem.check.adjoint(problem, x, u = NULL, tolerance = NULL)
}
\arguments{
\item{manifold}{A manifold specification.}

\item{x}{A feasible point. For the manifold check, NULL generates one.}

\item{u}{Optional nonzero tangent direction; otherwise generated randomly.}

\item{tolerance}{Relative error tolerance. NULL uses the point precision.}

\item{problem}{Scalar or least-squares problem.}

\item{steps}{Positive finite-difference step sizes; NULL uses a precision-aware grid.}
}
\value{
A `riem_check` list with status, checks, curves and details.
}
\description{
Numerical checks return evidence, not a proof of correctness. Directions
should be repeated at several interior points. Compact fixed-rank manifold
checks materialize dense matrices for the retraction derivative comparison. Unsupported Taylor tests are
reported as inconclusive, never silently replaced by another metric.
}
\examples{
if (torch::torch_is_installed()) {
  M <- manifold.sphere(3)
  x <- riem.random(M, device = "cpu")
  riem.check.manifold(M, x)$checks
  P <- riem.problem(M, function(x) -x[1])
  riem.check.gradient(P, x)$status
  riem.check.hessian(P, x)$status
  L <- riem.problem.leastsquares(manifold.euclidean(2), function(x) x * 2)
  riem.check.adjoint(L, torch::torch_ones(2, dtype = torch::torch_float64()))$status
}
}
