% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem-structured.R
\name{riem.problem.leastsquares}
\alias{riem.problem.leastsquares}
\title{Least-Squares Objectives and Metric Adjoints}
\usage{
riem.problem.leastsquares(
  manifold,
  residual,
  weights = NULL,
  jvp = NULL,
  adjoint = NULL,
  rhess = NULL,
  data = NULL,
  loss = "linear",
  loss_scale = 1,
  preconditioner = NULL,
  required_operations = NULL,
  value_rgrad = NULL
)
}
\arguments{
\item{manifold}{Geometry specification.}

\item{residual}{Function `residual(x)` returning a one-dimensional tensor.}

\item{weights}{Optional nonnegative vector of diagonal residual weights or
symmetric positive-semidefinite weight matrix, on the point's device/dtype.}

\item{jvp}{Optional residual differential callback `jvp(x, u)`.}

\item{adjoint}{Optional metric adjoint callback `adjoint(x, v)`. Both
differential callbacks must be supplied together, or both omitted.}

\item{rhess}{Optional exact Hessian callback for the full objective.}

\item{data}{Optional registered data passed to callbacks as a named `data`
argument.}

\item{loss}{Robust residual loss: linear, huber, soft_l1 or cauchy.}

\item{loss_scale}{Positive residual scale.}

\item{preconditioner}{Optional tangent preconditioner; see [riem.problem()].}

\item{required_operations}{Device operations required by callbacks.}

\item{value_rgrad}{Optional fused full-objective callback; see [riem.problem()].}
}
\value{
A `riem_leastsquares` problem. Linear loss has objective 0.5 r' W r.
  Robust losses apply scale squared times rho((r/scale)^2) componentwise
  before diagonal weighting. The Gauss--Newton model uses nonnegative rho'
  weights and omits rho'' terms.
}
\description{
Least-Squares Objectives and Metric Adjoints
}
\details{
Without callbacks, reverse-mode differentiation computes the metric
adjoint, and reverse-over-reverse differentiation computes J u. This needs
differentiable backward rules for residual operations. GN and LM use J* W J,
which is explicitly an approximation to the objective's exact Hessian.
}
\examples{
if (torch::torch_is_installed()) {
  P <- riem.problem.leastsquares(manifold.euclidean(2), function(x) x - 1)
  x <- torch::torch_zeros(2, dtype = torch::torch_float64())
  riem.optimize(P, x, "levenberg_marquardt")
}
}
