% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold-elementary.R
\name{manifold.oblique}
\alias{manifold.oblique}
\alias{manifold.multinomial}
\title{Oblique Matrices and the Fisher--Rao Simplex}
\usage{
manifold.oblique(p, k)

manifold.multinomial(p, metric = "fisher_rao")
}
\arguments{
\item{p}{Ambient dimension (number of rows or probabilities).}

\item{k}{Number of oblique columns.}

\item{metric}{Simplex metric, currently only `"fisher_rao"`.}
}
\value{
A geometry specification.
}
\description{
Oblique Matrices and the Fisher--Rao Simplex
}
\details{
Oblique columns have unit norm and the product round metric.
The simplex has strictly positive entries summing to one and metric
sum(u*v/x), equivalent to a radius-two sphere under `2*sqrt(x)`.
The simplex retraction is an exponential-coordinate normalization.
Both geometries provide exact exponential and local logarithm maps. The
simplex exponential rejects geodesics that leave its positive orthant.
}
\examples{
if (torch::torch_is_installed()) {
  M <- manifold.oblique(3, 2)
  x <- riem.random(M)
  u <- riem.tangent(M, x, torch::torch_randn_like(x)) * 0.1
  riem.log(M, x, riem.exp(M, x, u))

  S <- manifold.multinomial(4)
  riem.random(S)
}
}
