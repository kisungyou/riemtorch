% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold-elementary.R
\name{manifold.hyperbolic}
\alias{manifold.hyperbolic}
\title{Hyperbolic Ball and Hyperboloid}
\usage{
manifold.hyperbolic(d, model = c("poincare", "hyperboloid"), curvature = -1)
}
\arguments{
\item{d}{Intrinsic dimension.}

\item{model}{`"poincare"` or `"hyperboloid"`.}

\item{curvature}{Strictly negative sectional curvature.}
}
\value{
A manifold with d ball coordinates or d+1 hyperboloid coordinates.
}
\description{
Hyperbolic Ball and Hyperboloid
}
\details{
Hyperboloid uses signature (-,+,...,+) and the positive-time sheet.
Ball metric is 4/(1-c*||x||^2)^2 times Euclidean, where c=-curvature.
Retractions are local additive (ball) or timelike normalization (hyperboloid).
Exact exponential and logarithm maps are also available for both models.
}
\examples{
if (torch::torch_is_installed()) {
  M <- manifold.hyperbolic(2, "hyperboloid")
  x <- riem.random(M)
  u <- riem.tangent(M, x, torch::torch_randn_like(x)) * 0.05
  y <- riem.exp(M, x, u)
  riem.log(M, x, y)
}
}
