% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optim-training.R
\name{optim_rlinesearch}
\alias{optim_rlinesearch}
\title{Armijo Riemannian Training Optimizer}
\usage{
optim_rlinesearch(
  params,
  lr = 1,
  manifold = NULL,
  metric_weight = 1,
  backtrack = 0.5,
  armijo = 1e-04,
  max_backtracks = 25L
)
}
\arguments{
\item{params}{Tensor, list of tensors, or torch parameter groups. A group can
supply `manifold`, `lr`, `metric_weight`, and unique character `keys`.
A missing manifold means Euclidean geometry with the parameter's shape.}

\item{lr}{Positive learning rate.}

\item{manifold}{Default geometry for each tensor parameter.}

\item{metric_weight}{Positive factor metric weight, shared by group parameters.}

\item{backtrack}{Step reduction factor in (0,1).}

\item{armijo}{Sufficient decrease coefficient in (0,1).}

\item{max_backtracks}{Maximum reductions before failure.}
}
\value{
A torch optimizer. `step(closure)` requires a deterministic closure
  that clears gradients, computes a real scalar loss, calls backward and
  returns the loss. Failed searches restore all parameter values and gradients.
}
\description{
Armijo Riemannian Training Optimizer
}
\details{
Closures must not change buffers, random state, or external state.
  Each search varies all parameter groups together. Sparse gradients are not
  supported by this optimizer. Parameter identity is preserved.
}
\examples{
if(torch::torch_is_installed()) {
  x <- torch::nn_parameter(torch::torch_ones(2,dtype=torch::torch_float64()))
  opt <- optim_rlinesearch(list(x))
  opt$step(function() {opt$zero_grad();z<-x$square()$sum();z$backward();z})
}
}
