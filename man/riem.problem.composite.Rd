% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/problem-broader.R
\name{riem.problem.composite}
\alias{riem.problem.composite}
\title{Smooth Plus Nonsmooth Manifold Problems}
\usage{
riem.problem.composite(
  manifold,
  fn = NULL,
  nonsmooth,
  prox,
  egrad = NULL,
  rgrad = NULL,
  label = NULL,
  data = NULL,
  required_operations = NULL,
  qualified = TRUE
)
}
\arguments{
\item{manifold}{Geometry specification.}

\item{fn}{Function of one point returning one finite scalar torch tensor.}

\item{nonsmooth}{Function returning h(x), a real scalar tensor (positive
infinity can encode infeasibility), or a list of such functions for cyclic
proximal point. In that case `fn` must be NULL.}

\item{prox}{Function `prox(q, step)` solving exactly
argmin_y h(y)+d(y,q)^2/(2*step), or a matching list for cyclic proximal point.}

\item{egrad}{Optional ambient gradient callback `egrad(x)`.}

\item{rgrad}{Optional metric gradient callback `rgrad(x)`, mutually exclusive
with `egrad`.}

\item{label}{Optional problem label.}

\item{data}{Optional registered data. When supplied, callbacks receive it as
an additional named `data` argument. Registered tensor trees can be moved
with the problem by the execution driver.}

\item{required_operations}{Device operations required by callbacks; NULL
conservatively probes all. See [riem.devices()].}

\item{qualified}{Whether to require an initially qualified geometry. TRUE
supports Euclidean, affine, log-Euclidean positive/SPD, hyperbolic and their
products, powers and fixed metric scalings. FALSE permits experimental use
on other geometries with exact exponential, logarithm and distance maps.}
}
\value{
A composite `riem_problem` for `"proximal_gradient"` or
  `"cyclic_proximal_point"`.
}
\description{
Smooth Plus Nonsmooth Manifold Problems
}
\details{
A proximal callback is a mathematical contract; ambient shrinkage
  followed by projection generally does not satisfy it. Callbacks must use
  the declared metric (including scale and product weights). The proximal
  gradient solver uses an exponential gradient step and intrinsic prox,
  with sufficient-decrease backtracking. Cyclic proximal point uses a
  diminishing step sequence and reports a cycle residual, which alone is
  not a stationarity certificate for a sum of nonsmooth functions.
}
\examples{
if(torch::torch_is_installed()) {
  P <- riem.problem.composite(manifold.euclidean(2),
    function(x) ((x-2)^2)$sum()/2, nonsmooth=function(x) x$abs()$sum(),
    prox=function(q,step) q$sign()*(q$abs()-step)$clamp(min=0))
  riem.optimize(P,torch::torch_zeros(2,dtype=torch::torch_float64()),
                "proximal_gradient",device="cpu")$point
}
}
