% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifold-extensions.R
\name{manifold.power}
\alias{manifold.power}
\alias{manifold.scaled}
\title{Repeated and Scaled Manifold Geometries}
\usage{
manifold.power(manifold, copies)

manifold.scaled(manifold, scale)
}
\arguments{
\item{manifold}{Base manifold with tensor points.}

\item{copies}{Positive number of repeated factors.}

\item{scale}{Positive distance multiplier; the metric is multiplied by scale squared.}
}
\value{
A manifold specification. Power points have shape `c(copies, base_shape)`.
  Those factors belong to one optimization point; additional leading axes
  still denote independent batches. Scaling preserves the base representation.
}
\description{
Repeated and Scaled Manifold Geometries
}
\examples{
if (torch::torch_is_installed()) {
  M <- manifold.power(manifold.sphere(3), 4)
  x <- riem.random(M,device="cpu")
  riem.belongs(M,x)
  manifold.scaled(manifold.sphere(3),2)
}
}
